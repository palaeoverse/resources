The brand file `_brand.yml` was last changed in `palaeoverse/resources` on $DATE: $COMMIT_URL

Run `quarto use brand palaeoverse/resources` to keep the `_brand` folder in sync with the one in `palaeoverse/resources`.

---

*This issue is opened and kept up to date automatically by the `Notify pkgdown template` workflow in `palaeoverse/resources`. As long as it stays open, every new change to `_brand.yml` refreshes its title and body instead of opening a new issue.*
